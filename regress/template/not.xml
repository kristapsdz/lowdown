<!DOCTYPE html>
<html>
	<head>
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta charset="utf-8" /> 
		<title>foo</title>
	</head>
	<body>
		[$ifdef(not())$yes$endif$]
		[$ifdef(not)$yes$endif$]
		[$ifdef(not(title))$no$endif$]
		[$ifdef(not(and(title,authors)))$no$endif$]
		[$ifdef(not(and(title,)))$yes$endif$]
	</body>
</html>
